# Project brief

Prepare a local, reversible demonstration using only files inside the shared workspace.

Required fact: the verification code is `ORBIT-417`.

Constraints:

- Do not access credentials.
- Do not send or submit anything externally.
- Preserve source attribution when copying the verification code.
